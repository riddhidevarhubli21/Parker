import React, {useState, useEffect} from 'react'; /* these are react features that are used, to store and update data we use useState and when something changes useEffect*/
import { Text, View, TextInput, FlatList, TouchableOpacity, StyleSheet } from 'react-native';
import MapView, {Polyline, PROVIDER_GOOGLE} from "react-native-maps";

export default function ParkingScreen() {
  return (
    <View style = {StyleSheet.cotainer}>
      {/* App Title*/}
      
    </View>
  );
}
